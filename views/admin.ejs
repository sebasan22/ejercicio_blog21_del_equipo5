<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <%-include("partial/bsLink")%>
  </head>
  <body>
    <div class="container"><%-include("partial/NavBar")%></div>

    <div class="container">
      <h1>Estas en Admin</h1>
      <a href="/usuarios/crear" class="btn btn-success">Nuevo</a>
    </div>
    <div class="container">
      <table class="table">
        <thead>
          <tr>
            <th scope="col">Id</th>
            <th scope="col">Title</th>
            <th scope="col">Content</th>
            <th scope="col">Image</th>
            <th scope="col">Created at</th>
            <th scope="col">Author</th>
            <th scope="col">Actions</th>
          </tr>
        </thead>
        <!-- js incriptado, sacar el "/" de </%:

        <tbody>
          <tr>
            </%for(let article of articles){%>
            <thead>
              <th scope="row"></%=article.id%></th>
              <td></%=article.title%></td>
              <td></%=article.content%></td>
              <td></%=article.image%></td>
              <td></%=article.date%></td>
              <td></%=article.author%></td>
              <td>
                <a href="/usuarios/editar/</%=article.id%>" class="btn btn-primary">Editar</a
                ><a
                  type="button"
                  href="/usuarios/eliminar/</%=article.id%>"
                  class="btn btn-danger"
                  name="delete"
                  id="delete"
                >
                  Eliminar
                
              </td>
            </thead>
            </%}%>
          </tr>
        </tbody></a>-->
      </table>
    </div>
    <%-include("partial/footer")%> <%-include("partial/bsscript")%>
  </body>
</html>
